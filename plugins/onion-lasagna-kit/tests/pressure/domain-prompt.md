# Domain Skill Pressure Scenario

Model a `Project` aggregate for an Onion Lasagna backend. A project has an id, a name invariant, a created-by user id, and emits an event when created.

Include the domain files and tests you would create.
