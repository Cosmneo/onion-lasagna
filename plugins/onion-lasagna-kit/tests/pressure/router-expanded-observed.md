# Expanded Router Observed Behavior

The expanded router covers design, bounded-context structure, domain modeling, use-case work, route
work, adapters, and review without naming missing skills. In the pressure run it selected a design
or context-structure skill for the broad context request, then routed the aggregate, command, and
endpoint requests to the focused implementation skills while preserving inspect-before-assume.
