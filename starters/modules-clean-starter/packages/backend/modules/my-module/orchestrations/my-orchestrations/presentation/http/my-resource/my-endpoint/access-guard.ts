/**
 * Access Guard
 *
 * Define authorization rules for this endpoint:
 * - requiredRoles: Roles that can access this endpoint
 * - requiredPermissions: Specific permissions needed
 * - customGuard: Custom authorization logic function
 */
