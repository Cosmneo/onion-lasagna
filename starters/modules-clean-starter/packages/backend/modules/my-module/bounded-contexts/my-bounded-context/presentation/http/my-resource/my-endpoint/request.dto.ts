/**
 * Request DTO
 *
 * Define the shape of incoming request data:
 * - Body: Request body schema
 * - Query: Query parameters schema
 * - Params: Path parameters schema
 * - Headers: Required headers schema
 *
 * Extend BaseDto from onion-lasagna for automatic validation.
 * Use your chosen validator (zod, valibot, arktype, typebox) for schema definition.
 */
