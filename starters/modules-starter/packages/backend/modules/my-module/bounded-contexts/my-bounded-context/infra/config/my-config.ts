/**
 * Configuration
 *
 * Environment-specific configuration for this bounded context:
 * - Database connection settings
 * - External API URLs and keys
 * - Feature flags
 * - Timeout and retry settings
 */
