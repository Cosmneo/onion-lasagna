/**
 * Service Metadata
 *
 * Define service-level HTTP configuration for this bounded context:
 * - basePath: The root path for all routes (e.g., '/api/users')
 * - version: API version prefix (e.g., 'v1')
 * - tags: OpenAPI tags for documentation grouping
 * - middleware: Service-wide middleware configuration
 */
