/**
 * Outbound Port
 *
 * Interface for bounded context communication:
 * - Define methods to call bounded context use cases
 * - Implemented by adapters in infra layer
 * - Keeps orchestration layer pure
 */
