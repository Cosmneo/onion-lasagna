/**
 * Outbound Port (Repository Interface)
 *
 * Interface for infrastructure dependencies:
 * - Define repository methods
 * - Implemented by outbound adapters in infra layer
 * - Allows app layer to remain pure
 */
